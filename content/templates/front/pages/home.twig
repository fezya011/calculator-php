{% extends "layout.twig" %}

{% block styles %}
    <link href="/public/assets/front/css/home.css" rel="stylesheet">
{% endblock %}

{% block content %}
    <!-- Hero Section - White Version -->
    <section class="hero-section">
        <div class="container">
            <div class="hero-content">
                <div class="hero-badge">
                    🚀 Flat File CMS
                </div>
                <h1 class="hero-title">
                    Создавайте актуальные статьи
                </h1>
                <p class="hero-description">
                    Мощная система управления контентом на чистом PHP.
                    Быстро, безопасно и просто. Идеально для блогов,
                    лендингов и небольших сайтов.
                </p>
                <div class="hero-actions">
                    <a href="/articles" class="btn btn-primary btn-lg">
                        <i class="fi-rr-book-open me-2"></i>
                        Читать статьи
                    </a>
                    <a href="/about" class="btn btn-outline-dark btn-lg">
                        <i class="fi-rr-info me-2"></i>
                        Узнать больше
                    </a>
                </div>
            </div>
            <div class="hero-visual">
                <div class="code-window">
                    <div class="window-header">
                        <div class="window-dots">
                            <span></span>
                            <span></span>
                            <span></span>
                        </div>
                        <div class="window-title">article.md</div>
                    </div>
                    <div class="code-content">
                        <pre><code>---
title: "Моя статья"
category: "Programming"
date: "2024-01-15"
---

# Заголовок статьи

Содержание в **Markdown**...</code></pre>
                    </div>
                </div>
            </div>
        </div>
        <div class="hero-background">
            <div class="gradient-orbit"></div>
            <div class="gradient-orbit"></div>
            <div class="gradient-orbit"></div>
        </div>
    </section>

    <!-- Контент страницы -->
    {% if content %}
        <div class="page-content mb-40">
            {{ content|raw }}
        </div>
    {% endif %}

    <!-- Последние статьи -->
    <div class="section-articles mb-50">
        <h2 class="section-title">
            Последние статьи
        </h2>

        {% if articles %}
            <div class="articles-grid">
                {% for article in articles %}
                    <article class="article-card">
                        {% if article.meta.cover_image %}
                            <div class="article-image" style="background-image: url('{{ article.meta.cover_image|e('html_attr') }}');"></div>
                        {% else %}
                            <div class="article-image" style="background: linear-gradient(135deg, #1a1a1a 0%, #404040 100%);"></div>
                        {% endif %}

                        <div class="article-content">
                            <div class="article-header">
                                <div class="article-title">
                                    <h3 class="h4 mb-0">
                                        <a href="/article/{{ article.slug|e }}">
                                            {{ article.title|e }}
                                        </a>
                                    </h3>
                                </div>
                                {% if article.meta.category %}
                                    <div class="article-category">
                                        {{ article.meta.category|e }}
                                    </div>
                                {% endif %}
                            </div>

                            <div class="article-meta">
                                <span class="meta-item">
                                    <i class="fi-rr-user me-1"></i>{{ (article.meta.author ?? 'Автор')|e }}
                                </span>
                                <span class="meta-item">
                                    <i class="fi-rr-calendar me-1"></i>{{ article.meta.date ? article.meta.date|date('d.m.Y') : 'Не указана' }}
                                </span>
                                <span class="meta-item">
                                    <i class="fi-rr-time me-1"></i>
                                    ~{{ ((article.content|striptags|split(' ')|length) / 200)|round(0, 'ceil') }} мин
                                </span>
                            </div>

                            <p class="article-excerpt">
                                {{ (article.excerpt ?? article.content|striptags|slice(0, 150) ~ '...')|e }}
                            </p>

                            <a href="/article/{{ article.slug|e }}" class="article-button">
                                Читать статью
                            </a>
                        </div>
                    </article>
                {% endfor %}
            </div>

            <div class="text-center mt-30">
                <a href="/articles" class="btn btn-primary">
                    Смотреть все статьи
                </a>
            </div>
        {% else %}
            <div class="empty-state">
                <div class="empty-icon">📭</div>
                <h3 class="h4 mb-10">Статьи пока не добавлены</h3>
                <p class="mb-20">Администратор еще не опубликовал ни одной статьи.</p>
                <a href="/articles" class="btn btn-outline-primary">Обновить</a>
            </div>
        {% endif %}
    </div>

    <!-- Особенности -->
    <div class="section-features">
        <h2 class="section-title">
            Возможности платформы
        </h2>

        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">📝</div>
                <h3 class="feature-title">Простое редактирование</h3>
                <p class="feature-description">
                    Создавайте и редактируйте статьи в формате Markdown с удобным редактором
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📂</div>
                <h3 class="feature-title">Категории</h3>
                <p class="feature-description">
                    Организуйте статьи по категориям для удобной навигации
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">⚡</div>
                <h3 class="feature-title">Быстрая работа</h3>
                <p class="feature-description">
                    Оптимизированная система без лишних зависимостей
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🔒</div>
                <h3 class="feature-title">Безопасность</h3>
                <p class="feature-description">
                    Защита от XSS, CSRF атак. Хранение данных в файлах
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🎨</div>
                <h3 class="feature-title">Красивый дизайн</h3>
                <p class="feature-description">
                    Современный адаптивный дизайн с анимациями
                </p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🚀</div>
                <h3 class="feature-title">Легкий запуск</h3>
                <p class="feature-description">
                    Установка за 5 минут. Не требует базы данных
                </p>
            </div>
        </div>
    </div>
{% endblock %}

{% block java_script %}
    <script src="/public/assets/front/js/readTime.js"></script>
{% endblock %}